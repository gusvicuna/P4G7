What happens if you add a comment at the end of a line?
A
Nothing, the comment is added normally
The code isn't going to run
The line of code transforms in a comment
The comment becomes part of the code
Comments are ignored by the compiler